% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_rda.R
\name{train_rda}
\alias{train_rda}
\title{Train a Regularized Discriminant Analysis (RDA) classifier}
\usage{
train_rda(train, test, cv_splits, settings)
}
\arguments{
\item{train}{tibble/data.frame with training rows. Must contain
\code{metagenre}, \code{artist.s.id}, \code{track.s.id}, \code{case_wts} (if
\code{settings$use_caseweights} TRUE) and features in
\code{settings$model_features}.}

\item{test}{tibble/data.frame with hold-out test rows.}

\item{cv_splits}{resampling object used for tuning (can be NULL).}

\item{settings}{list of settings. Expected entries include \code{seed},
\code{model_features}, \code{use_caseweights}, \code{n_cores}, \code{n_cores_tuning},
\code{tune_gamma}, \code{tune_lambda}, \code{gamma_fix}, \code{lambda_fix},
\code{tune_downsample}, \code{tune_upsample}, and Bayesian tuning params.}
}
\value{
A list with elements: \code{model}, \code{tuning_history}, \code{evaluation},
\code{model_settings}.
}
\description{
Train and evaluate a Regularized Discriminant Analysis (RDA) model
using the klaR implementation. Supports optional Bayesian tuning of
\code{gamma} and \code{lambda} regularization parameters and sampling-ratio
tuning. Conforms to the project's case-weight and CV conventions.
}
\examples{
\dontrun{
settings <- list(
  seed = 42, model_features = c("f1","f2"),
  use_caseweights = TRUE,
  tune_gamma = TRUE, tune_lambda = TRUE,
  gamma_fix = 0.5, lambda_fix = 0.5
)
out <- train_rda(train, test, cv_splits, settings)
}
}
