% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_adaptive_sampling.R
\name{step_adaptive_sampling}
\alias{step_adaptive_sampling}
\title{Adaptive Class Balancing Recipe Step}
\usage{
step_adaptive_sampling(
  recipe,
  ...,
  role = NA,
  trained = FALSE,
  target_ratio = 5,
  seed = NULL,
  skip = TRUE,
  id = recipes::rand_id("adaptive_sampling")
)
}
\arguments{
\item{recipe}{A recipe object}

\item{...}{Column selector for the outcome variable (typically the class)}

\item{role}{Not used by this step}

\item{trained}{Logical, has the step been trained}

\item{target_ratio}{Numeric, controls class balance (1 = perfect balance,
higher values retain more original imbalance)}

\item{seed}{Random seed for reproducibility}

\item{skip}{Logical, should step be skipped when baking new data (default
TRUE - sampling should only apply to training data)}

\item{id}{Unique identifier for the step}
}
\value{
An updated recipe object
}
\description{
A recipe step that adaptively balances class distributions using NearMiss
for downsampling and Borderline-SMOTE (with SMOTE fallback) for upsampling.
The step uses a single tunable target_ratio parameter that controls the
degree of class rebalancing.
}
\details{
The algorithm:
\enumerate{
\item Computes a reference count from class frequencies using an adaptive
quantile (median for ≤10 classes, 60th percentile for 11-20 classes,
65th percentile for >20 classes)
\item Downsamples classes exceeding reference × target_ratio using NearMiss
\item Upsamples classes below reference ÷ target_ratio using Borderline-SMOTE
(falls back to regular SMOTE if Borderline-SMOTE fails)
}

With skip = TRUE (default), the sampling is only applied during prep()
on training data, not when bake() is called on new data.
}
