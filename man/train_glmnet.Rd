% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_glmnet.R
\name{train_glmnet}
\alias{train_glmnet}
\title{Train a multinomial GLMNET classifier for metagenre prediction}
\usage{
train_glmnet(train, test, cv_splits, settings)
}
\arguments{
\item{train}{A tibble/data.frame with training rows. Must contain
columns: \code{metagenre}, \code{artist.s.id}, \code{track.s.id}, \code{case_wts}
(when \code{settings$use_caseweights} is TRUE) and the features named in
\code{settings$model_features}.}

\item{test}{A tibble/data.frame with hold-out test rows.}

\item{cv_splits}{Resampling object (e.g. result of
\code{create_artist_cv_splits()} or \code{rsample::vfold_cv}) used for tuning.
Can be NULL when no tuning is requested.}

\item{settings}{List of modelling settings. Required entries include
\code{seed}, \code{model_features}, \code{use_caseweights}, \code{penalty_fix},
\code{alpha_fix}, \code{tune_penalty}, \code{tune_alpha}, \code{tune_downsample},
\code{tune_upsample}, \code{under_ratio_fix}, \code{over_ratio_fix}, \code{n_cores},
\code{n_cores_tuning}, \code{initial_grid_size}, \code{bayes_iterations},
\code{uncertain_jump}.}
}
\value{
A list with elements:
\itemize{
\item \code{model}: fitted workflow object (final fitted model)
\item \code{tuning_history}: tuning history tibble or NULL
\item \code{evaluation}: list with confusion matrices and metrics for train
and test sets
\item \code{model_settings}: list with chosen hyperparameters and seed
}
}
\description{
Train and evaluate a multinomial elastic-net model using tidymodels.
Supports optional Bayesian tuning of penalty, mixture and sampling
ratios using provided resampling splits. Respects case weights stored
in \code{case_wts}, artist-based CV splits, and a reproducible seed.
}
\examples{
\dontrun{
settings <- list(
  seed = 42,
  model_features = c("f1", "f2"),
  use_caseweights = TRUE,
  penalty_fix = 1e-4,
  alpha_fix = 0.5
)
out <- train_glmnet(train, test, cv_splits, settings)
}
}
