% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_network_graph.R
\name{plot_static_tree_graph}
\alias{plot_static_tree_graph}
\title{Plot Static Genre Tree Graph for Publication}
\usage{
plot_static_tree_graph(
  graph,
  sizes_lookup,
  fill_lookup,
  layout = c("horizontal", "vertical"),
  width = NULL,
  height = NULL,
  font_size = 10,
  horizontal_label_levels = NULL,
  spacing_factor = 0,
  margin_left = 100,
  margin_right = 100,
  margin_top = 50,
  margin_bottom = 50,
  output_file = NULL
)
}
\arguments{
\item{graph}{An igraph object representing the hierarchical structure}

\item{sizes_lookup}{Named numeric vector. Node sizes lookup. Tag names as names.}

\item{fill_lookup}{Named character vector. Node fill colors lookup.}

\item{layout}{Character. Either "horizontal" or "vertical".}

\item{width}{Numeric or NULL. Width of the plot in pixels. If NULL or too
small, automatically calculated to prevent label overlap.}

\item{height}{Numeric or NULL. Height of the plot in pixels. If NULL or too
small, automatically calculated to prevent label overlap.}

\item{font_size}{Numeric. Font size for labels in pt.}

\item{horizontal_label_levels}{Numeric or NULL. For vertical layout only:
number of hierarchy levels (from root) to display with horizontal labels.
If NULL, automatically detects levels without overlap.}

\item{spacing_factor}{Numeric. Interpolates between compact spacing (0) and
the original D3 spacing (1). Values outside this range extrapolate.}

\item{margin_left, margin_right, margin_top, margin_bottom}{Numeric. Margins
in pixels.}

\item{output_file}{Character or NULL. If provided, saves the plot as PNG.}
}
\value{
An r2d3 htmlwidget object. If output_file is provided, also saves
the plot as PNG.
}
\description{
Creates a static hierarchical tree graph suitable for publication.
Supports horizontal and vertical layouts with automatic label rotation.
}
\details{
Static features for publication:
\itemize{
\item Horizontal layout: labels displayed horizontally, splitting nodes on left,
leaf nodes on right
\item Vertical layout: labels rotated 90 degrees where needed to prevent overlap,
horizontal where space permits. Splitting nodes above, leaf nodes below.
\item Links displayed in 80\% grey with thickness based on edge weight
\item Splitting node labels in bold
\item All labels same size and black color
\item Dimensions auto-adjusted to prevent label overlap
}
}
\examples{
\dontrun{
plot_static_tree_graph(
  graph, sizes_lookup, fill_lookup,
  layout = "vertical",
  horizontal_label_levels = 2,
  output_file = "tree_plot.png"
)
}

}
